"""Flag tools: set_flags, mark_read/unread, star/unstar (M2)."""

from __future__ import annotations

# M2 stubs — implementations added in milestone 2


async def set_flags(*args, **kwargs):
    raise NotImplementedError("set_flags — M2")


async def mark_read(*args, **kwargs):
    raise NotImplementedError("mark_read — M2")


async def mark_unread(*args, **kwargs):
    raise NotImplementedError("mark_unread — M2")


async def star(*args, **kwargs):
    raise NotImplementedError("star — M2")


async def unstar(*args, **kwargs):
    raise NotImplementedError("unstar — M2")
