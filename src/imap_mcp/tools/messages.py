"""Message reading, searching, and listing tools (M1)."""

from __future__ import annotations

# M1 stubs — implementations added in milestone 1


async def read_email(*args, **kwargs):
    raise NotImplementedError("read_email — M1")


async def download_attachment(*args, **kwargs):
    raise NotImplementedError("download_attachment — M1")


async def search_emails(*args, **kwargs):
    raise NotImplementedError("search_emails — M1")


async def list_messages(*args, **kwargs):
    raise NotImplementedError("list_messages — M1")
